---
description: یک صفحه یا ویجت را کاملاً با M3 هماهنگ می‌کند (همهٔ قوانین با هم)
argument-hint: <مسیر فایل>
allowed-tools: Bash(dart run tool/m3_audit.dart:*), Bash(flutter analyze:*), Read, Edit, Glob, Grep
---

فایل `$1` را کاملاً با Material 3 هماهنگ کن — این‌بار همهٔ قوانین با هم،
نه یکی‌یکی. برای فایل‌های بزرگ و پرتخلف که تکه‌تکه کردنشان بی‌فایده است.

۱. کل فایل را بخوان.
۲. قبل از هر ویرایش، یک نقشه بده: چند مورد از هر قانون، و برای رنگ‌ها
   نگاشت پیشنهادی‌ات (`Colors.X در متن Y → semantic.Z`). منتظر تأیید بمان.
۳. بعد از تأیید، جدول نگاشت `CLAUDE.md` را کامل اعمال کن.
۴. `flutter analyze $1` و شمارش audit را بررسی کن.

قواعد سختگیرانه:
- رفتار و منطق تغییر نمی‌کند، فقط ظاهر
- هیچ ویجتی حذف یا جابه‌جا نمی‌شود مگر M3 صریحاً جایگزین دارد
  (`ElevatedButton` → `FilledButton` مجاز است؛ بازچینش layout نه)
- هر موردی که معنایش مبهم است دست‌نخورده می‌ماند و گزارش می‌شود
