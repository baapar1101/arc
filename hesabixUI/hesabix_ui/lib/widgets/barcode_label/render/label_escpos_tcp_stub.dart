Future<void> sendEscPosOverTcp({
  required String host,
  required int port,
  required List<int> data,
}) async {
  throw UnsupportedError(
    'ارسال ESC/POS خام روی وب پشتیبانی نمی‌شود؛ از نسخه ویندوز یا اندروید استفاده کنید.',
  );
}
