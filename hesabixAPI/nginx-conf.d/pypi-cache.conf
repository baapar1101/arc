# PyPI reverse-proxy cache (keys_zone=pypi_cache — used by p.mirror.hesabix.ir)
proxy_cache_path /var/cache/nginx/pypi
    levels=1:2
    keys_zone=pypi_cache:100m
    max_size=50g
    inactive=3650d
    use_temp_path=off;
