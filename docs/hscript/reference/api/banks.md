---
doc_id: hscript.api.banks
title: API: banks / warehouses / persons
tags: [api, banks, warehouses, persons]
version: 1
---

# banks
- `banks.filter(search?, limit?)`
- `banks.all(limit?)`

فیلدها: `id`, `code`, `name`, `branch`, `account_number_masked`, `balance`, `is_active`, `is_default`

# warehouses
- `warehouses.filter(search?, limit?)`
- `warehouses.all(limit?)`

# persons
- `persons.filter(search?, person_type?, limit?)`
- `persons.all(limit?)`
